services:
  lhr:
    image: ghcr.io/jahanzaib-kaleem/lhr:latest
    pull_policy: always
    container_name: lhr
    restart: unless-stopped
    ports:
      - "127.0.0.1:${LHR_STUDIO_PORT:-8787}:8787"
      - "127.0.0.1:${LHR_MCP_PORT:-8788}:8788"
    volumes:
      - lhr-data:/data

volumes:
  lhr-data:
    name: ${LHR_DATA_VOLUME:-lhr-data}
