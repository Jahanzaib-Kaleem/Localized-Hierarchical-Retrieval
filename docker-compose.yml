services:
  lhr:
    image: ghcr.io/jahanzaib-kaleem/lhr:latest
    container_name: lhr
    restart: unless-stopped
    ports:
      - "127.0.0.1:8787:8787"
    volumes:
      - lhr-data:/data

volumes:
  lhr-data:
